<%= form_with(model: [:admin, category]) do |form| %>
  <div class="field">
    <%= form.label :title, '分類', class: 'label' %>
    <%= form.text_field :title, class: 'input is-medium' %>
  </div>
  <%= form.submit class: 'button is-primary is-medium' %>
<% end %>
